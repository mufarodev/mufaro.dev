// place files you want to import through the `$lib` alias in this folder.

export { heroState } from './stores/hero-state.svelte';

export { default as HeroPill } from './components/hero-pill.svelte';
